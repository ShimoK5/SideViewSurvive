using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpringEffectSoro : MonoBehaviour
{
    // Start is called before the first frame update
    public void MyDestroy()
    {
        Destroy(gameObject);
    }
}
